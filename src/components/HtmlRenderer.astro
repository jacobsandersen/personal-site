---
interface Props {
    html: string,
    class: string[]
}

const { html, class: className } = Astro.props as Props
---

<div class:list={["prose", "prose-blue", "dark:prose-invert", "prose-headings:font-serif", "prose-headings:my-4", ...className]} set:html={html} />
