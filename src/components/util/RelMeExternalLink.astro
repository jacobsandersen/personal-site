---
import ExternalLink from './ExternalLink.astro';

interface Props {
    href: string;
}

const { href } = Astro.props as Props
---

<ExternalLink href={href} showIcon={false} linkProps={{rel: "me"}}><slot /></ExternalLink>