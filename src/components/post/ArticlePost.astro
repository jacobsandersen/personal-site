---
import { Article } from '~/lib/content'
import ContentRenderer from '../content/ContentRenderer.astro'

interface Props {
    article: Article
    preview?: boolean
}

const { article, preview = false } = Astro.props as Props
---

<ContentRenderer content={article.content} preview={preview} />