---
import HFeedRenderer from "~/components/microformat/hfeed/HFeedRenderer.astro";
import PageTitle from "~/components/PageTitle.astro";
import BaseLayout from "~/layouts/BaseLayout.astro";
import DefaultBastionClient from "~/lib/bastion";
import { PostMf2Type } from "~/types/bastion";

const { category } = Astro.params;
if (!category) {
  return Astro.redirect("/400")
}

const posts = await DefaultBastionClient.posts().type(PostMf2Type.H_ENTRY).tag(category).query()
console.log(posts)

const title = `Posts tagged with #${category}`;
---

<BaseLayout title={title}>
    <PageTitle 
      title={title} 
      crumbs={["Home:/", "Post Tags:/tagged", `#${category}:/tagged/${category}`]} />
    <HFeedRenderer
        feed={posts}
        feedInfo={{ title, url: `/tagged/${category}`, headerConfig: { level: 2, hidden: true } }}
        class="tw:grid tw:grid-cols-2 tw:gap-4"
        itemClass="tw:my-4"
    />
</BaseLayout>
