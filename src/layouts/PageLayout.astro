---
import SiteHeader from '~/components/SiteHeader.astro'
import BaseLayout from './BaseLayout.astro'

interface Props {
    title?: string
}

const { title } = Astro.props as Props
---

<BaseLayout pageTitle={title}>
    <SiteHeader shouldMarkupAuthor={false} />
    <main>
        <slot />
    </main>
</BaseLayout>