---
import BaseLayout from './BaseLayout.astro'

interface Props {
    title?: string
}

const { title } = Astro.props as Props
---

<BaseLayout pageTitle={title}>
    <main>
        <slot />
    </main>
</BaseLayout>