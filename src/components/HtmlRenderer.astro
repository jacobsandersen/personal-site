---
interface Props {
    html: string,
    class: string[]
}

const { html, class: className } = Astro.props as Props
---

<div class:list={["tw:prose", "tw:prose-blue", "tw:dark:prose-invert", "tw:prose-headings:font-serif", "tw:prose-headings:my-4", ...className]} set:html={html} />
